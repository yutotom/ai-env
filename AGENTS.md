# AGENTS.md

このディレクトリは、`uv` による Python 環境構築と依存関係解決の挙動を検証するための作業場所です。

## 基本方針

- 思考は英語で行い、ユーザーへの出力は日本語で行う。
- 変更はこの検証ディレクトリ内に限定する。
- 既存のユーザー変更を勝手に戻さない。
- `uv` の挙動確認では、実行したコマンドと観測された結果を簡潔に記録する。

## 検証対象

主に以下を確認する。

- `pyproject.toml` の `requires-python` と `.python-version` の関係
- `uv lock` / `uv sync` / `uv run` の Python バージョン選択
- Python バージョン制約に合わない依存パッケージを追加したときの解決失敗
- `uv.lock` に反映される `requires-python` と依存関係の変化
- 仮想環境 `.venv` の再作成や更新が必要になる条件

## 作業時の注意

- ファイル検索には原則として `rg` / `rg --files` を使う。
- 手動編集には `apply_patch` を使う。
- 依存関係の追加や lock 更新など、ネットワークや環境変更を伴う可能性がある操作では、失敗内容を確認してから必要に応じて再実行する。
- `.venv` や `uv.lock` の変更は、検証目的に沿う場合のみ行う。
- Python の互換性検証では、次の情報を明確にする。
  - 現在の `.python-version`
  - `pyproject.toml` の `requires-python`
  - 実際に `uv` が選択した Python
  - 成功または失敗した `uv` コマンド

## 推奨する確認コマンド

```bash
uv --version
uv python list
uv run python --version
uv lock
uv sync
```

## 回答スタイル

- 結果は日本語で、要点から短く伝える。
- バージョン制約の不整合がある場合は、どのファイルのどの指定が衝突しているかを具体的に示す。
- 次に試すべき検証がある場合は、実行コマンドを添えて提案する。
